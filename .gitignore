# Credentials — never commit these
.env
.env.local

# config.js holds only the public Supabase URL + anon key (safe to expose —
# RLS protects all data). It is committed so Cloudflare can serve it.

# macOS
.DS_Store

# Editor
.vscode/
.idea/

# Local Cloudflare Pages Functions dev cache (wrangler pages dev)
.wrangler/
